{
	"name": "cursor-position-history",
	"version": "1.1.0",
	"description": "Remember the previous positions of the cursor across multiple files in Obsidian: auto-scroll to the correct position in a new file. Navigate backward and forward through your cursor position history without losing context.",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
		"version": "node version-bump.mjs && git add manifest.json versions.json"
	},
	"keywords": [],
	"author": "",
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^16.11.6",
		"@typescript-eslint/eslint-plugin": "^8.25.0",
		"@typescript-eslint/parser": "^8.25.0",
		"builtin-modules": "^4.0.0",
		"esbuild": "^0.25.0",
		"obsidian": "^1.8.7",
		"tslib": "^2.8.1",
		"typescript": "^5.7.3"
	}
}
